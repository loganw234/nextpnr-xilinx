# Route for routability alone: router2 reads timing_driven, and with it off
# neither arc criticality nor the per-iteration timing analysis enters. On
# cft-fp256's pre-fix board placement nextpnr's own estimate was 19.8 MHz
# against the 100 MHz asked, so nearly every arc was critical and the timing
# term only added cost; the frozen placement's estimate is in its place.log.
timing_driven=0
