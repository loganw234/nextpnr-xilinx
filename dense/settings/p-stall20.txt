# HeAP's stall limit at 20 (upstream 5): on the bench netlist the analytic
# loop stops at iteration 11 and keeps iteration 6, its solver wirelength
# at 59% of the legal one (the loop's own target is 80%). This lets it
# run on; it still keeps the best legal wirelength. For dense/place.sh.
NEXTPNR_PLACER_MAX_STALL=20
